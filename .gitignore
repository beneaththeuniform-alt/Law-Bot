# Never commit these — they contain secrets or are machine-specific
venv/
local_secrets.py
__pycache__/
*.pyc
*.pdf
Note.txt
Other/
.streamlit/secrets.toml

# The vector database SHOULD be committed (not ignored) — your cloud
# deployment needs this actual data to answer questions. Do not add
# chroma_db/ to this file.